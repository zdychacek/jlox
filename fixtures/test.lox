class Thing {
  init(name) {
    this.name = name;
  }
  getCallback() {
    fun localFunction() {
      print(this.name);
    }

    return localFunction;
  }
}

var me = Thing("Ondrej");
var callback = me.getCallback();

callback();
